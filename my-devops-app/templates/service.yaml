apiVersion: v1
kind: Service
metadata:
  name: webserver-static-service
spec:
  type: NodePort
  selector:
    app: {{ .Values.appLabel }}
  ports:
    - port: 80
      targetPort: 80
      nodePort: 32000
